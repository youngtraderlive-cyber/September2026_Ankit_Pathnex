- name: Intall Helm
   hosts: all
   become: yes
      tasks:
         - name: Download Helm
           git url:
             url: https.........................
             dest: /tmp/helm/.tar.gz
       - name: Extact helm
         unarchive:
            src: /tmp/local/bin/
            remote_src: yes
      - name: Set Helm Binary Permissions
        file:
         path: /use/local/bin/helm
         mode: '0755'


resource: "aws_eip" "pathnex_eip" {
  instance = aws_instance.pathnex_ec2.id
}

resource "aws_intance" "pathnex_ec2" {
    ami = "ami-0abcd1234abcd1234"
    instance_type = "t3.medium"
 
  tags = { 
    name = "pathnex-EC2"
 }


helm repo add stable https://chart.helm.sh/stable
helm install pathnex-nginx stable/nginx-ingress

pipeline {
  agent any
   environment { 
    IMAGE_NAME = 'Jio-web-app"
}
  stage {
    stage('checkout') {
     steps { 
      git 'https://github.com/pathnex/sample-repo.git'
     }
}
  stage('Build Docker Image') {
    steps {
       script {
            docker.build("${env.image_name}")
        }  
    
      }
    }


  stsage('Push Docker Image') {
    stpes {
      script {
   
           docker.writeRegistrt(https://docker.io','docker-credentials') {
          docker.image("${env.image_name}").push('lastes')
           }
        }
     }       
  }   


 stage('Deploy to Kubernetes') {
  steps {
    script {
      sh 'kubectl apply -f deployment.yaml'
            }     
          }
        }
     }
   }    

stage:
  - build
  - push
  - deploy

build:
 stage: build
 script:
   - docker build -t pathnex-web-app.


push:
 stage: push
   script:
      - docker login -u GitLab CI/CD — Build, Push, and Deploy with Helm
stages:
  - build
  - push
  - deploy

build:
  stage: build
  script:
    - docker build -t pathnex-web-app .

push:
  stage: push
  script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD"
    - docker push pathnex-web-app

deploy:
  stage: deploy
  script:
    - helm repo add stable https://charts.helm.sh/stable
    - helm install pathnex-nginx stable/nginx-ingress

docker
from nginx 
expoe 80 
docker run -d -p 8080:80 nginx











